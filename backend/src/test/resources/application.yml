spring:
  datasource:
    url: jdbc:h2:mem:kylinops-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    placeholder-replacement: true
    placeholders:
      # 默认 CLOB：H2 PG 模式 + @Lob 期望 CLOB 类型
      lob_type: CLOB
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

kylinops:
  audit:
    enabled: true
    page-size: 50
  executor:
    whitelisted-services:
      - nginx
      - mysql
      - redis
      - ssh
      - docker